import { RoutePlaceholder } from '../../../routes/RoutePlaceholder'
import { routeMetadata } from '../../../routes/routeMetadata'

export function CheckoutPage() {
    return <RoutePlaceholder heading={routeMetadata.checkout.heading} />
}

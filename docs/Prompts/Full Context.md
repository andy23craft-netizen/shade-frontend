# Full Project Context

You are working on the Shade frontend.

Before responding to my task:

1. Read `docs/AGENTS.md` in full and treat it as the baseline project context and operating guidance.
2. Inspect the current repository and relevant documentation instead of assuming that the baseline is still current.
3. Check the working tree before editing, and preserve all unrelated user changes.
4. Consult the relevant requirements, API documentation, plan, and feature ticket when the task depends on them.
5. Call out conflicts, missing information, or stale documentation rather than silently inventing behavior.

Follow the change workflow and verification guidance in `docs/AGENTS.md`. Keep changes focused, update tests when
behavior changes, and update project context when the repository's architecture, commands, dependencies, files, or
runtime flow materially change.

Once you have loaded the context, complete the task I provide.

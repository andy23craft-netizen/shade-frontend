# Public, non-secret deployment inputs. Copy this file to .env and replace
# placeholders with deployment values; never put credentials here.
SHADE_LIBRARY_HOST_DOMAIN=example.test
SHADE_API_PROXY=1
SHADE_API_PROXY_TARGET=http://127.0.0.1:8000
SHADE_API_UPSTREAM=backend:8000
